export type IContentCard = {
  name: string;
  image: string;
  author: string;
  subtext: string;
  completed: string;
  duration: string;
  org: string;
};

export type ImageAreaProps = {
    imageUrl: string;
}